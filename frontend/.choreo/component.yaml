# +required Version of the file
schemaVersion: 1.0

# +required Name of the component
componentName: menugen-frontend
componentDisplayName: MenuGen Frontend
componentDescription: Frontend application for MenuGen - AI-powered menu visualization

# +required Type of the component
componentType: WebApplication

# +optional Port the app is listening on
port: 8080

# +optional Ingress configuration for the component
ingress:
  include: "**"
